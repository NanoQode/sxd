/**
 * Location explorer entry point: the interactive Nigeria market explorer
 * (map, list, filters, comparison, scenario calculators) for the homepage
 * (`variant="homepage"`, compact) and the /explore page (`variant="full"`).
 */
export { LocationExplorer, type LocationExplorerProps } from './location-explorer';
