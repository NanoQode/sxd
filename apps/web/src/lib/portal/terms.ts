/**
 * Version label recorded with every quote acceptance (build brief §8: the
 * acceptance stores the signature name, terms version, IP hash and agent).
 * Bump when the customer terms change so old acceptances stay attributable.
 */
export const CUSTOMER_TERMS_VERSION = 'customer-terms-2026-09';
export const CUSTOMER_TERMS_PATH = '/policies/terms';
