/**
 * Engagement records: per-kind rules for engagement items (due-diligence
 * checklists, findings, queries, red flags, closing checklists...) and the
 * content rules for reports written under a service request. Pure functions.
 */
export * from './items';
export * from './reports';
